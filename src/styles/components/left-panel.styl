@import 'nib';
@import '../vars.styl';

.left-panel
  display inline-flex
  background-color orchid
  flex-direction column
  height 100%
  max-width 150px
  min-height 100%
  overflow-x hidden
  overflow-y auto
  width 15%
  flex 1 1 auto

  .table-tab, .favorite-tab
    > div, select
      display inline-flex
      width 125px
    > button
      background-color blue
